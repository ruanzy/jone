<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<sql id='count'>
		select count(*) from goods_unit
	</sql>
	<sql id='selectAll'>
		select * from goods_unit order by id DESC
	</sql>
	<sql id='add'>
		insert into goods_unit(id,name) values(#{id},#{name})
	</sql>
</sqls>