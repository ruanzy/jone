<?xml version="1.0" encoding="UTF-8"?>
<sqls>
	<sql id='count'>
		select count(*) from receive
		<where>
			<if test="customer != null and customer != ''">
				and customer in(select id from customer where name like CONCAT('%',#{customer},'%'))
			</if>
		</where>
	</sql>
	<sql id='selectAll'>
		select * from receive
		<where>
			<if test="customer != null and customer != ''">
				and customer in(select id from customer where name like CONCAT('%',#{customer},'%'))
			</if>
		</where>
	</sql>
</sqls>