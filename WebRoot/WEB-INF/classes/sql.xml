<?xml version="1.0" encoding="UTF-8"?>
<commands>
	<sql id='count'>
	 <![CDATA[
		select count(*) from users
	 	where id>-1
		<#if username??>
			and username like '%${username}%'
		</#if>
		<#if state??>
			and state=${state}
		</#if>
	  ]]>
	</sql>
	<sql id='selectAll'>
	 <![CDATA[
		select * from users
	 	where id>-1
		<#if username??>
			and username like '%${username}%'
		</#if>
		<#if state??>
			and state=${state}
		</#if>
		order by id desc
	  ]]>
	</sql>
</commands>